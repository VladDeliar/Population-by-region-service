# Which data source `get_data` parses and `print_data` prints.
# Available values: wikipedia | statisticstimes
#
# To use: copy this file to `.env` — docker-compose picks it up automatically:
#   cp .env.example .env
SOURCE=wikipedia

# To switch back to the Wikipedia source, set instead:
# SOURCE=wikipedia
